import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.turnuvaist.taekwondo',
  appName: 'Turnuvaist Taekwondo',
  webDir: 'dist',
};

export default config;
